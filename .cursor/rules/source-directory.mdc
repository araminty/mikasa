---
description: Edit source/ only; out/ is generated build output
alwaysApply: true
---

# Source vs out

- Edit game logic in `source/scenes/**/*.scene.dry` and `source/qdisplays/*.qdisplay.dry` only (see `game-source-only.mdc`).
- Do not hand-edit `out/` for game logic — it is regenerated by `npm run make-html` (see `game-source-only.mdc`).
- Verify changes with a local build when behavior matters.
- `/robots.txt` (repo root) — ask crawlers not to index `source/`, `out/`, `scripts/`, `.cursor/` on GitHub.
- `source/robots.txt` — same policy for the playable build; copy to `out/html/robots.txt` when publishing.
